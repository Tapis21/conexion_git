<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MiAgendaEscolar.Pages.InicioPage"
             Title="Inicio">

    <ScrollView>
        <StackLayout Padding="30" Spacing="25">
            <Label Text="¡Bienvenido a MiAgenda Escolar!"
                   FontSize="28"
                   HorizontalOptions="Center"
                   TextColor="#333"/>

            <Image Source="home.png"
                   HeightRequest="150"
                   HorizontalOptions="Center" />

            <Label Text="¿Qué deseas hacer hoy?"
                   FontSize="20"
                   TextColor="DarkSlateGray"/>

            <Label Text="🧾 Ver tus materias registradas"
                   FontSize="16"/>
            <Label Text="📌 Revisar tus tareas pendientes"
                   FontSize="16"/>
            <Label Text="📅 Consultar tu calendario semanal"
                   FontSize="16"/>
            <Label Text="📝 Escribir una nota rápida"
                   FontSize="16"/>

            <Button Text="Ir a Materias"
                    Clicked="IrMaterias_Clicked"
                    BackgroundColor="#1976D2"
                    TextColor="White"
                    CornerRadius="10"/>

        </StackLayout>
    </ScrollView>
</ContentPage>